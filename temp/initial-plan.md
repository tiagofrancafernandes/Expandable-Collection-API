me ajude a melhorar meu prompt para eu passar para o codex criar um plano de execução e começar a executar.
Segue meu prompt:

a tenant database for collection data

cada usuário poderá criar uma coleção do tipo que quiser e a api retornará json com os dados

imagino que cada collections seja um josnb gigante mas pode fazer da melhor forma em termo de performance

podem haver filtros

- alguns tipos de dados aceito:
    - tipos escalares classicos (int, foat, boolean, string, null)
    - lista (json/array)
    - fks (referências para outras collections)
    - internal_function -> exemplo now(), int_val(value|0), float_val(value|0.0), str_uuid4_gen(), int_big_gen(), int_short_gen(), int_between(start|0, end|1000), float_between(start|0, end|1000), str_random(length|8), json_as_json(string), etc

Quero que alguns tipos possam ter dados gerados automaticamente (pretendo usar o tipo internal_function com a FN em questão) como:
    - hora atual
    - int (não trabalharemos com id auto increment então o INT gerado será aleatório ou valor passado)


digamos que um usuário cria uma collection 'posts' com o schema parecido com o abaixo:

postsSchema:
  id?: UUID (string), unique, AUTOGENERATED,
  title: string
  slug: string
  published: boolean, default: false
  content: longText (string)
  tags: string[]
  author: Ref[authorSchema], nullable
  cover_image: Ref[mediaSchema], , nullable

authorSchema:
  id?: UUID (string), unique, AUTOGENERATED,
  name: string
  slug: string

mediaSchema:
  id?: UUID (string), unique, AUTOGENERATED,
  file_name: string
  original_file_name?: string, nullable
  alt_label?: string, nullable
  mime_type?: string, nullable
  size?: number, nullable
  disk?: string, nullable
  path?: string, nullable
  storage?: string, nullable
  path?: string, nullable
  external_url?: string, nullable
  public?: boolean, nullable, default(true)

-----

cada usuário terá seus proprios dados e preciso garantir que ninguém acesse os dados dos outros.

tecnologias que pretendo usar:
- Laravel
- PostgreSQL

Nesse momento sem front, apenas a API

A aplicação precisa ter testes automatizados para cada lógica importante e testes para os endpoints

pretendo usar spatie/laravel-permissions para separar papeis e permissões. Pretend trabalhar com a lógica permission based acces control e usar policies já que poderei ter regras de segurança mais específicas. Garanta global scope para usuário que não seja o 'super-admin' do sistema.

Garanta ter factory e seeders para as models criadas.

Crie usuários iniciais:
super admin: admin@mail.com, senha power@123

Usuário1: user1@mail.com, senha power@123 -> algumas collections criadas
Usuário2: user2@mail.com, senha power@123 -> livre

Meu objetivo principal é ter performance (uso de cache nas respostas indepotentes ajudará)

------------

Algo próximo do que eu espero:

GET /api/v1/collections/{collection} -> lista os dados da collection (deve aceitar paginação e filtros por propriedades)
GET /api/v1/collections/{collection}/schema -> retorna o schema do banco em formato json
GET /api/v1/collections/{collection}/text -> retorna o schema do banco em formato plain text
GET /api/v1/collections/{collection}/schema/dts -> retorna o schema do banco em formato .d.ts (útil se quiser salvar para um projeto)

GET /api/v1/collections/{collection}/{record}/show -> retorna o registro (se existir) em formato json
PUT /api/v1/collections/{collection}/{record}/update -> atualiza o registro (se existir) e retorna atualizado em formato json (deve atualizar o cache para esse registo e o do index da collection também)
DELETE /api/v1/collections/{collection}/{record}/delete -> excluir o registro (se existir)


Quero outros endpoints como:
- criação de collection
- delete de collection
- criação de collection de midia
- upload para collection de midia
- delete de item da collection de midia
- update de item da collection de midia (nome, visibilidade etc)
- criação e listagem de tokens para collections
    - por exemplo poderá criar um token para uma coleção que só tem 'read' útil para frontend e outrs com o CRUD completo para um painel customizado onde poderá gerenciar os dados
    - poder criar um token por projeto e as permissões por collection escolhidas na criação ou adiconadas posteriomente. Exemplo:
        - projetoA token1:
            - collection abc: [full]
            - collection def: [full]
            - collection xyz: [full]
            - collection ghi: [create, read, update, delete]

        - projetoA token2:
            - collection abc: []
            - collection def: [create, read]
            - collection xyz: [read]
            - collection ghi: [full]

        - projetoA token3:
            - collection abc: [read]
            - collection def: [read]
            - collection xyz: [read]
            - collection ghi: [read]
    - só para ficar claro a estrutura que espero:
        - um usuário tipo TENANT (registrado no sistema e posteriormente pagará pelo recuroso) tem PROJETOS
            - um PROJETO tem
                - COLLECTIONS -> estrutura que permite dados no formato que o cliente quiser sem precisar criar migrations para isso. Sempre que um dado for ser inserido ou retornado da colletcion o 'schema' dessa collection será checado para inserção e formatado para saída assim garantimos uma estrutura aceitável tanto na entrada quanto no retorno e para evitar sobrecarga de recursos sempre formatando saída, um cache será feito para o registro ao ser inserido, listado ou atualiado de forma que evita reavaliação de registros e sempre que isso ocorrer será preciso revalidar o cache da listagem da mesma collection evitando dado atualizado em um local e desatualizado em outro
                    - COLLECTION tem:
                        - COLLECTION_RECORDS -> registro no banco que permite inserção de dados estruturados de forma rápida de leitura, filtro etc
                            - para COLLECTION_RECORDS acho que colocar tudo em uma coluna tipo json/jsonb seja o ideal até certo ponto mas isso pode se tornar um problema com muitos dados e filtros complexos, então acho que criar uma coluna adicional que faça armazenamento de alguma forma binária que o banco entenda pode ser útil ou no momento da criação/edição da colletcion poder selecionar colunas filtráveis e ordenáveis (basicamente idexável) assim sempre que o dado for atualizado/inserido pode ser criado um resumo que poder ser útil para filtros, ordenações (só acho, mas pode implementar uma estratégia alternativa se for o melhor)
                - MEDIA_COLLECTIONS -> coleção especial exclusiva para midias como imagens, vídeos e arquivos (com tipos/mime_type e tamanhos máximos controlados)
- administrativos

----------
Quero também que a aplicação permita criação de formulários para collections onde o
----------
sobre as skils que precisa se atentar:

conhecimento de policies, gate e controle de acesso com o Laravel.
preciso que me ajude com recursos nativos do Laravel e se eu mencionar pacotes de terceiros, garanta que implementei adequadamente dentro do padrão Laravel.
Quero que use a documentação oficial e foque nas versões 10 em diante

---------------------

quero que a aplicação também gere documentação de API automaticamente então pode instalar algum pacote atualizado que gere essas informações.

----
preciso que o readme.md aponte para outro arquivo que também deve ser criado: README.md dentro de uma pasta chamada 'dev' e nessa mesma pasta conterá outros .md com instruções de uso, instalação, exemples etc. Coisas úteis para os devs que vierem a trabalhar com o projeto futuramente.

-----

Crie um arquivo AGENTS.md com a estrutura e descrição do projeto e coisas pertinentes ao projeto.

-----
